# Production Environment Variables
# Used during build for static export

# Site URL (HTTPS only)
NEXT_PUBLIC_SITE_URL=https://crm.medlemsregistret.se

# API Base URL (same as site in static export)
NEXT_PUBLIC_API_BASE_URL=https://crm.medlemsregistret.se

# Environment
NODE_ENV=production

# Static export mode
NEXT_OUTPUT=export
NEXT_ENABLE_STATIC_EXPORT=true

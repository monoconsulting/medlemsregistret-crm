# CRM Backend (Loopia deployment) miljövariabler

# Server
PORT=4000
ALLOWED_ORIGINS=https://example.com,https://admin.example.com

# JWT / session
JWT_SECRET=byt-mig-till-en-lång-och-slumpad-sträng
SESSION_COOKIE_NAME=crm_session
SESSION_COOKIE_DOMAIN=.example.com
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_SAME_SITE=none
SESSION_TTL_DAYS=7

# Databas
DATABASE_URL=mysql://user:password@mysql513.loopia.se:3306/databasnamn

# Sök/AI-tjänster (valfria)
# SEARCH_PROVIDER=typesense
# TYPESENSE_HOST=typesense.example.com
# TYPESENSE_PROTOCOL=https
# TYPESENSE_API_KEY=nyckel
# AI_PROVIDER=anthropic
# ANTHROPIC_API_KEY=nyckel
